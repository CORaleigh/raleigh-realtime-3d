export interface Stats {
    vehicles: number;
    avgSpeed: number;
    driving: number;
  }


  export interface FilterValue {
    label: string;
    where: string;
    selected: boolean;
    image: string;
  }